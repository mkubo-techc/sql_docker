SQL課題一覧（このファイルに追記する形で対応）

以下のSQLを流したうえで回答してみてください
`docker\mysql\init.sql`の68-87行目
`docker\mysql\sql\insert_orders.sql`
`docker\mysql\sql\insert_order_items.sql`
`docker\mysql\sql\insert_users.sql`


## 1. 条件に合致するレコードを抽出しよう（WHERE）
「products」テーブルから価格が500円以上の商品の一覧を取得せよ。
- 500円以上の商品一覧を抽出

【解答】


## 2. 部分一致で検索しよう（LIKE）
「users」テーブルから名前に「田」が含まれているユーザーの名前とメールアドレスを取得せよ。

- 名前に「田」が含まれるユーザーを検索

【解答】


## 3. 集計関数とGROUP BYを使ってみよう
「orders」テーブルを使って、ユーザーごとの合計購入金額を表示せよ。

- 各ユーザーの購入金額合計を算出

【解答】

## 4. HAVINGで条件付きグルーピング
注文回数が2回以上のユーザーIDとその注文数を表示せよ（GROUP BY＋HAVING）

- 注文回数が多いユーザーを抽出

【解答】

## 5. 日付関数で月別集計しよう
注文日を月ごとに集計して、月ごとの注文数を表示せよ。

- 月別注文数を算出（strftimeなど使用）

【解答】

## 6. 価格帯別にカテゴリ分け（CASE）
「products」テーブルで、商品を以下の3カテゴリに分類し、商品名とカテゴリを表示せよ。

- 1000円以上 →「高級」
- 500円〜999円 →「標準」
- 499円以下 →「安価」

商品価格に応じてカテゴリ分け（CASE）

【解答】

## 7. サブクエリで最新注文を取得
「orders」テーブルから、最も新しい注文を1件だけ取得せよ。

- サブクエリ or ORDER BY LIMITで最新注文

【解答】

## 8. 新規テーブル追加（仕様追加パターン）
新たに「reviews（レビュー）」テーブルを追加しよう。以下の仕様でinit.sqlに追加するSQLを記述せよ。

- review_id（PK）
- user_id（INTAGER）
- product_id（INTAGER）
- comment（TEXT）
- score（整数、1〜5）

reviewsテーブルのCREATE TABLE文を記述

【解答】

※`docker\mysql\sql\insert_review.sql`でreviewsテーブルにデータを入れることが可能です

## 9. 外部キー制約を理解しよう（JOIN設計）
上記で作成した「reviews」テーブルと「users」「products」テーブルをJOINして、レビュー情報（ユーザー名、商品名、スコア）を一覧表示せよ。

- 3つのテーブルをJOINしてレビュー一覧を表示

【解答】

## 10. 関数＋加工でランキングを作ろう
「orders」テーブルをもとに、購入金額上位3名のユーザーIDと合計金額を表示せよ。

- 購入金額ランキングTOP3

【解答】


## 11. 商品ごとのレビュー平均スコアを計算しよう（AVG＋GROUP BY）
-- reviewテーブルを使って、商品ごとの平均スコアを計算せよ。
-- 平均スコアが4.0以上の商品のみ表示。

-- 商品IDごとの平均スコアを算出
-- HAVINGで平均スコアが4.0以上

【解答】


## 12. ユーザーごとの総レビュー数を求めよう（COUNT＋GROUP BY）
-- reviewテーブルから、ユーザーごとのレビュー数をカウントせよ。
-- レビュー数が3件以上のユーザーのみ表示。

-- ユーザーIDごとのレビュー件数をカウント
-- HAVINGで3件以上のユーザー抽出

【解答】


## 13. 月別売上集計をしてみよう（SUM＋GROUP BY）
-- ordersテーブルから、月ごとの売上金額合計を求めよ。
-- 売上が50万円以上の月のみ表示。

-- 月単位にまとめる
-- SUMで売上合計
-- HAVINGで50万円以上

【解答】


## 14. 注文キャンセル率を求めよう（COUNT＋CASE＋集計）
-- ordersテーブルを使って、
-- ユーザーごとの注文キャンセル率を算出せよ。
-- （キャンセル件数 ÷ 総注文数 × 100）
-- キャンセル率が30%以上のユーザーのみ抽出。

-- CASE式を使ってキャンセル件数を集計
-- COUNTで総注文数カウント

【解答】


## 15. 注文の平均単価を求めよう（SUM, COUNT, AVG）
-- order_itemsテーブルから、商品ごとの平均販売単価を計算せよ。
-- 平均単価が3000円以上の商品のみ表示。

-- 商品IDごとのAVG単価計算
-- HAVINGで3000円以上フィルター

【解答】


## 16. 購入頻度が高いユーザーを探せ（COUNT＋GROUP BY）
-- ordersテーブルを使って、ユーザーごとの注文回数をカウントせよ。
-- 回数が5回以上のユーザーを表示。

-- 注文回数カウント
-- HAVINGで5回以上

【解答】


## 17. レビューコメントの文字数を計測しよう（LENGTH＋AVG）
-- reviewテーブルで、ユーザーごとにレビューコメントの平均文字数を求めよ。
-- 平均文字数が30文字以上のユーザーのみ抽出。

-- LENGTHで文字数取得
-- AVGで平均文字数計算
-- HAVINGで30文字以上フィルター

【解答】


## 18. 最もレビュー数が多い商品を探せ（COUNT＋GROUP BY＋ORDER BY）
-- reviewテーブルから、商品ごとのレビュー数をカウントせよ。
-- レビュー数が多い順に上位3商品を表示。

-- 商品ごとのレビュー数カウント
-- ORDER BYで降順ソート
-- LIMIT 3

【解答】

## 19. レビューを書いたことのあるユーザーを取得しよう（DISTINCT）
-- reviewテーブルから、レビューを書いたユーザーIDを重複なしで取得せよ。

-- DISTINCTを使って重複除去

【解答】


## 20. 注文金額順に並び替えよう（ORDER BY）
-- ordersテーブルから、注文ID、ユーザーID、注文金額を
-- 注文金額が高い順に並び替えて表示せよ。

-- ORDER BY total_price DESC

【解答】


## 21. 注文履歴の一部を取得しよう（OFFSET＋FETCH）
-- ordersテーブルから、注文履歴を金額順に並び替えた上で、
-- 6件目から5件だけ取得せよ。

-- ORDER BY total_price DESC
-- OFFSET 5 ROWS FETCH NEXT 5 ROWS ONLY

【解答】